name: CI/CD Pipeline Bourbonomics

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch:
    inputs:
      stage:
        description: "Optional SST stage override — prod, stg, or dev (leave blank to use branch mapping)"
        required: false
        default: ""
        type: string

# Avoid stacking deploys for the same ref.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ----------------------------------------------------------------------
  # CI: type-check, test, build. Acts as the gate for the deploy job.
  # ----------------------------------------------------------------------
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      # `sst install` generates `.sst/platform/config.d.ts`, which
      # sst.config.ts references via a triple-slash directive. Without
      # this step, tsc would fail on the missing type reference. It
      # runs offline — no AWS calls — so it's safe pre-deploy.
      - name: Bootstrap SST platform types
        run: npx sst install

      - name: Type-check
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build (Next.js)
        run: npm run build

  # ----------------------------------------------------------------------
  # Stage resolver:
  #   main → prod, stg → stg, dev → dev, anything else → dev.
  # workflow_dispatch input wins if set.
  # ----------------------------------------------------------------------
  stage:
    runs-on: ubuntu-latest
    outputs:
      stage: ${{ steps.map.outputs.stage }}
    steps:
      - id: map
        run: |
          set -e

          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            STAGE="prod"
          elif [ "${{ github.ref }}" = "refs/heads/stg" ]; then
            STAGE="stg"
          elif [ "${{ github.ref }}" = "refs/heads/dev" ]; then
            STAGE="dev"
          else
            STAGE="dev"
          fi

          if [ -n "${{ github.event.inputs.stage }}" ]; then
            case "${{ github.event.inputs.stage }}" in
              prod|stg|dev)
                STAGE="${{ github.event.inputs.stage }}"
                ;;
              *)
                echo "Invalid manual stage '${{ github.event.inputs.stage }}'"; exit 1
                ;;
            esac
          fi
          echo "stage=$STAGE" >> "$GITHUB_OUTPUT"

  # ----------------------------------------------------------------------
  # Deploy. Only runs on:
  #   - push to main → prod, push to stg → stg, push to dev → dev, or
  #   - workflow_dispatch (operator-driven).
  # PRs and arbitrary feature branches do NOT auto-deploy — push to `dev`
  # to promote a change.
  # ----------------------------------------------------------------------
  deploy:
    needs: [ci, stage]
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && (
        github.ref == 'refs/heads/main' ||
        github.ref == 'refs/heads/stg'  ||
        github.ref == 'refs/heads/dev'
      ))
    runs-on: ubuntu-latest
    env:
      HOSTED_ZONE_ID: ${{ secrets.HOSTED_ZONE_ID }}
      CERTIFICATE_ARN: ${{ secrets.CERTIFICATE_ARN }}
      DOMAIN: ${{ secrets.DOMAIN }}
      STAGE: ${{ needs.stage.outputs.stage }}
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      # SST 4.x bundles its own pulumi-language-nodejs; the Ubuntu
      # runner ships an older preinstalled copy that conflicts. Drop
      # it before deploying.
      - name: Remove preinstalled Pulumi language plugin
        run: sudo rm -f /usr/local/bin/pulumi-language-nodejs

      - name: Deploy with SST
        run: npx sst deploy --stage "${{ env.STAGE }}"

      # Invalidate CloudFront so the UI updates immediately. Edge
      # cache can otherwise serve old HTML for ~10min after deploy.
      - name: Invalidate CloudFront cache
        run: |
          case "${{ env.STAGE }}" in
            prod) SITE_DOMAIN="$DOMAIN" ;;
            stg)  SITE_DOMAIN="stg.$DOMAIN" ;;
            *)    SITE_DOMAIN="dev.$DOMAIN" ;;
          esac
          if [ -z "$DOMAIN" ]; then
            echo "DOMAIN secret empty, skipping invalidation"
            exit 0
          fi
          DIST_ID=$(aws cloudfront list-distributions --output json | jq -r --arg dom "$SITE_DOMAIN" '.DistributionList.Items[]? | select((.Aliases.Items // []) | index($dom)) | .Id' | head -1)
          if [ -n "$DIST_ID" ] && [ "$DIST_ID" != "null" ]; then
            aws cloudfront create-invalidation --distribution-id "$DIST_ID" --paths "/*"
            echo "Invalidation created for distribution $DIST_ID ($SITE_DOMAIN)"
          else
            echo "No CloudFront distribution found for $SITE_DOMAIN, skipping invalidation"
          fi
