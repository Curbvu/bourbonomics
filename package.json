{
  "name": "bourbonomics",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "sync:cards": "node scripts/sync-bourbon-catalog.mjs",
    "sync:investments": "node scripts/sync-investment-catalog.mjs",
    "sync:resource-cards": "node scripts/sync-resource-card-index.mjs",
    "prebuild": "npm run sync:cards && npm run sync:investments && npm run sync:resource-cards",
    "predev": "npm run sync:cards && npm run sync:investments && npm run sync:resource-cards",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3",
    "@aws-sdk/client-dynamodb": "^3",
    "@aws-sdk/util-dynamodb": "^3",
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/aws-lambda": "^8",
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "sst": "^4.3.2",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
